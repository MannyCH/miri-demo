/**
 * Maps native HTML interactive elements to their design system equivalents.
 * Derived from src/components/ — update this when a new component is added
 * that replaces a native element.
 */
export default {
  button: 'Button',
  input: 'TextField',
  select: 'SelectField',
};
